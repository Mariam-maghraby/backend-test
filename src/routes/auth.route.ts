import { Request, Response, NextFunction } from "express";

const post__auth_login = async (
  req: Request,
  res: Response,
  next: NextFunction,
) => {
  try {
  } catch (err) {
    next(err);
  }
};
